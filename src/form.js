// src form.js

// Placeholder function for building a form
function build_form(fields) {
    // Simple smoke test so you can see it working:
    const form = make('form', '', 'contact-form');

    return form;
}