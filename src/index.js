// src/index.js
function buildSite(site) {
  buildSections(site.sections);
}

// Choose wich site to show:
// buildSite(PAINTING_SITE);
buildSectionsAlt(COOKING_SITE.sections);
// buildSite(PORTFOLIO_SITE);


